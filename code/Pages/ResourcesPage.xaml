<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Catpaq.Pages.ResourcesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Catpaq.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- zpaqfranz 状态 -->
        <Grid Grid.Row="0" Padding="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="LblProcess" Grid.Column="0" Text="zpaqfranz: not running" VerticalAlignment="Center" />
            <Button x:Name="BtnKill" Grid.Column="1" Content="Kill zpaqfranz" Click="BtnKill_Click" />
        </Grid>

        <!-- 任务管理器式监控卡片 -->
        <Grid Grid.Row="1" Padding="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- CPU 卡片 -->
            <Border x:Name="CpuCard" Grid.Column="0"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                    CornerRadius="8" Padding="12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="80" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock x:Name="LblCpuTitle" Text="CPU" Foreground="Gray" FontSize="13" />
                    <TextBlock x:Name="LblCpuPct" Grid.Row="1" FontSize="42" FontWeight="Bold" Margin="0,2,0,4" />
                    <Grid x:Name="CpuPlot" Grid.Row="2">
                        <Canvas x:Name="CpuGrid" />
                        <!-- 虚线（带色）=系统总量；实线=zpaqfranz 占用；线下渐变涂色 -->
                        <Polygon x:Name="CpuTotalFill" />
                        <Polyline x:Name="CpuTotalLine"
                                  Stroke="{ThemeResource AccentFillColorDefaultBrush}"
                                  StrokeThickness="1.5"
                                  StrokeDashArray="4,3" />
                        <Polygon x:Name="CpuZpaqFill" />
                        <Polyline x:Name="CpuZpaqLine"
                                  Stroke="{ThemeResource AccentFillColorDefaultBrush}"
                                  StrokeThickness="2.5" />
                    </Grid>
                    <TextBlock x:Name="LblCpuDetail" Grid.Row="3" Foreground="Gray" Margin="0,4,0,0" TextWrapping="Wrap" />
                </Grid>
            </Border>

            <!-- 内存卡片 -->
            <Border x:Name="MemCard" Grid.Column="2"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                    CornerRadius="8" Padding="12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="80" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock x:Name="LblMemTitle" Text="Memory" Foreground="Gray" FontSize="13" />
                    <TextBlock x:Name="LblMemPct" Grid.Row="1" FontSize="42" FontWeight="Bold" Margin="0,2,0,4" />
                    <Grid x:Name="MemPlot" Grid.Row="2">
                        <Canvas x:Name="MemGrid" />
                        <!-- 虚线（带色）=系统总量；实线=zpaqfranz 占用；线下渐变涂色 -->
                        <Polygon x:Name="MemTotalFill" />
                        <Polyline x:Name="MemTotalLine"
                                  Stroke="{ThemeResource SystemFillColorSuccessBrush}"
                                  StrokeThickness="1.5"
                                  StrokeDashArray="4,3" />
                        <Polygon x:Name="MemZpaqFill" />
                        <Polyline x:Name="MemZpaqLine"
                                  Stroke="{ThemeResource SystemFillColorSuccessBrush}"
                                  StrokeThickness="2.5" />
                    </Grid>
                    <TextBlock x:Name="LblMemDetail" Grid.Row="3" Foreground="Gray" Margin="0,4,0,0" TextWrapping="Wrap" />
                </Grid>
            </Border>
        </Grid>

        <TextBlock x:Name="LblNote" Grid.Row="2" Text="" Foreground="Gray" Margin="8" />
    </Grid>
</Page>
